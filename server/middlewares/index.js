import validatePostOrder from './validatePostOrder';
import validateUpdateStatus from './validateUpdateStatus';

const middlewares = {
  validatePostOrder,
  validateUpdateStatus,
};

export default middlewares;
